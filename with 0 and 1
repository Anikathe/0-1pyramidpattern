#include<stdio.h>
void main(){
    int numRows=12;
    int row,col;
    for(row=0;row<=numRows;row++){
        for(col=0;col<=row;col++){
            int remainder=col%2;
            printf("%d",remainder);
        }
        printf("\n");
    }
}
